import { BaseService } from "../../shared/base";

export class ContinueTransaction extends BaseService {
    
    constructor() {
        super();
    }

    async execute(sessionId: string) {
    }
}